---
export interface Props {
  href: string;
}

const { href } = Astro.props;
---

<a href={href} class="inline-block bg-blue-600 hover:bg-blue-500 font-semibold py-3 px-4 rounded-lg focus:outline-none transition-all">
  <slot />
</a>